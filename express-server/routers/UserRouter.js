import {Router} from 'express';
// import { testFunc, generateRegistrationToken } from '../controllers/UserController.js';
// import { userAuth } from 'src/middlewares/AuthMiddlewares';

const userRouter = Router();

// userRouter.post('/generate-registration-link', generateRegistrationToken)

export default userRouter